{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix app",
  "buildCommand": "npm run build --prefix app && rm -rf .vercel/output && cp -r app/.vercel/output .vercel/output",
  "framework": "tanstack-start"
}
